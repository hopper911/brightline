/**
 * Shared ISR interval for public marketing pages (Phase 15B/15C).
 * Import this instead of `force-dynamic` on cacheable public routes.
 */
export { PUBLIC_PAGE_REVALIDATE_SECONDS } from "@/lib/public-chrome-cache";
